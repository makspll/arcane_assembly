{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ModDescriptor",
  "description": "Describes the contents of a mod",
  "type": "object",
  "properties": {
    "attachKind": {
      "$ref": "#/$defs/AttachKind"
    },
    "dependantOnLuaScripts": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "description": {
      "type": "string"
    },
    "kind": {
      "$ref": "#/$defs/ScriptKind"
    },
    "name": {
      "type": "string"
    },
    "spellComponents": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/SpellComponentDescriptor"
      }
    },
    "version": {
      "type": "string"
    }
  },
  "required": [
    "name",
    "kind",
    "description",
    "version",
    "attachKind",
    "dependantOnLuaScripts",
    "spellComponents"
  ],
  "$defs": {
    "AttachKind": {
      "type": "string",
      "enum": [
        "Player",
        "Static"
      ]
    },
    "ScriptKind": {
      "type": "string",
      "enum": [
        "Core",
        "User"
      ]
    },
    "SlotCount": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "None",
            "Unlimited"
          ]
        },
        {
          "type": "object",
          "properties": {
            "FixedAmount": {
              "type": "integer",
              "format": "uint",
              "minimum": 0
            }
          },
          "additionalProperties": false,
          "required": [
            "FixedAmount"
          ]
        }
      ]
    },
    "SpellComponentDescriptor": {
      "description": "A descriptor for a",
      "type": "object",
      "properties": {
        "area_of_effect_meters": {
          "description": "If a component has an area of effect, the engine will provide nearest entities/projectiles to its callbacks",
          "type": "number",
          "format": "float"
        },
        "children_slots": {
          "description": "Components can be slotted with children components, for example\na grouping component might trigger effects within its children components every frame",
          "$ref": "#/$defs/SlotCount",
          "default": "None"
        },
        "delay_milliseconds": {
          "description": "The delay after which the next component can be triggered following this one.\nIf less than the time between frames, will trigger a\ncomponent every frame, but no more frequently than that.",
          "type": "number",
          "format": "float"
        },
        "disable_collisions": {
          "description": "If false will spawn a collider on the spell and send collision events to the spell",
          "type": "boolean",
          "default": false
        },
        "handler_label": {
          "description": "The postfix to give to every handler for this spell. For example if this name is 'my_spell' then the handler for casting the spell\nwould have to be called \"on_cast_my_spell\", and similar for all the other spell callbacks.",
          "type": "string"
        },
        "identifier": {
          "description": "A unique (within the mod) spell name. Typically in snake case.",
          "type": "string"
        },
        "lifetime_milliseconds": {
          "description": "The time after which this component is killed, and its death effect triggered",
          "type": "number",
          "format": "float"
        },
        "mana_drain_per_shot": {
          "description": "The amount of mana drained every time this component is triggered.\nIf mana is not enough for the next component, firing is blocked.",
          "type": "number",
          "format": "float"
        }
      },
      "required": [
        "identifier",
        "handler_label",
        "mana_drain_per_shot",
        "delay_milliseconds",
        "lifetime_milliseconds",
        "area_of_effect_meters"
      ]
    }
  }
}